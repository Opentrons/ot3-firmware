# Rules for checking the security of our GitHub Actions with zizmor.

rules:
  # todo: Incrementally fix violations and remove `disable: true`.
  artipacked:
    disable: true
  cache-poisoning:
    disable: true
  excessive-permissions:
    disable: true
  secrets-outside-env:
    disable: true
  superfluous-actions:
    disable: true
  template-injection:
    disable: true
